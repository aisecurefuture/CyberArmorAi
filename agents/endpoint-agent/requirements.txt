# CyberArmor Endpoint Agent - Python Dependencies
# Core
psutil>=5.9.0
watchdog>=3.0.0
httpx>=0.25.0
pydantic>=2.0.0

# Cryptography
cryptography>=41.0.0

# Database (local policy/audit cache)
aiosqlite>=0.19.0

# Scheduling
apscheduler>=3.10.0

# Process name visibility — sets the kernel process title to "cyberarmor-agent"
# so ps/top/Activity Monitor/SIEM forwards see the right name instead of
# "python". Also prevents the agent's own process_monitor from accidentally
# classifying the agent itself as an external AI tool.
setproctitle>=1.3.0

# Cross-platform clipboard access for the macOS user-session clipboard
# helper (clipboard_helper.py + install_clipboard_helper.sh, LaunchAgent).
# Not used by the system LaunchDaemon — daemons can't read the user's
# pasteboard on macOS — but kept in the shared requirements so the agent
# venv has it ready when the helper is installed.
pyperclip>=1.8.0

# Platform-specific (optional)
# macOS: no additional deps (uses native frameworks)
# Windows: pywin32 (uncomment below)
# pywin32>=306; sys_platform == "win32"

# PQC (optional — falls back to X25519/Ed25519 if missing)
# liboqs-python>=0.9.0

# Deep packet inspection (optional)
# scapy>=2.5.0

# Network capture (optional)
# pyshark>=0.6

# Logging / telemetry
structlog>=23.0.0
